<Window x:Class="ComplexityCalculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ComplexityCalculator"
        mc:Ignorable="d"
        Title="Analisador de Complexidade de algoritmos C#" Height="450" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Digite abaixo o código C# que deseja analisar:"
                   Grid.Row="0"
                   FontSize="14"
                   Margin="0,0,0,5" FontStyle="Italic" HorizontalAlignment="Left" Foreground="#FF353434"/>

        <TextBox x:Name="CodeInput"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 Grid.Row="1"
                 FontFamily="Consolas"
                 FontSize="14"/>

        <Button Content="Analisar Código"
                Grid.Row="2"
                Margin="0,10,0,0"
                Click="Analyze_Click"/>

        <TextBlock x:Name="ResultOutput"
                   Grid.Row="3"
                   FontSize="16"
                   FontWeight="Bold"
                   Foreground="DarkBlue"
                   Background="AliceBlue"
                   Margin="0,10,0,0"
                   TextWrapping="Wrap"/>
    </Grid>
</Window>
